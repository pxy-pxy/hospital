<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.itgaoshu.Mapper.UpplierMapper">
    <select id="findAddUpplier" parameterType="Upplier">
        select * from Upplier
        <where>
            <if test="supplierName!=null and supplierName!=''  ">
                and supplierName like concat('%',concat(#{supplierName},'%'))
            </if>
        </where>
    </select>
    <insert id="addUpplier" parameterType="Upplier">
         insert into Upplier(supplierName)values(#{supplierName})
    </insert>
    <update id="deleteUpplier" parameterType="Integer">
        update Upplier set  supplierName=#{supplierName} where supplierId=#{supplierId}
    </update>
    <select id="count" resultType="int">
        select count(*) from Upplier where supplierName=#{supplierName}
    </select>
</mapper>